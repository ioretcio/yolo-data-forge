[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
empty_pictures = "empty_pictures.empty_pictures:empty_pictures"
train_val_split = "train_val_split.train_val_split:train_val_split"
big_image_to4parts = "big_image_to4parts.big_image_to4parts:big_image_to4parts"
dataset_fingerprint = "dataset_fingerprint.fingerprint_creator:fingerprint_creator"
similarity = "simalar_images_finder.similarity_test:similarity_test"
db_intersect = "db_folder_intersection.db_folder_intersection:intersector"

[project]
name = "data-managment-toolbox"
version = "0.0.4"
authors = [
  { name="ioretcio", email="goncharukyou@example.com" },
]
description = "a knife for data"
requires-python = ">=3.6"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]